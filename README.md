Python 练习项目

怎么跑
1. 安装依赖：`pip install requests python-dotenv`
2. 在根目录创建 `.env`，写入：`DEEPSEEK_API_KEY=你的key`
3. 运行：
   - 对话工具：`python chat.py "你的问题"`
   - 文件统计：`python tool.py .`

需要什么环境
- Python 3.10+
- 需要 DeepSeek API key（去 platform.deepseek.com 申请）
- `.env` 文件需自己创建，不包含在仓库里