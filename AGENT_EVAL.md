# Agent 评测报告

## 背景

- **Agent**：手写 ReAct 循环，不依赖框架
- **工具集**（4 个）：`search_docs`（RAG 检索）、`read_file`（读文件）、`write_file`（写文件）、`list_files`（列目录）
- **任务集**：10 道多步任务，围绕 `game_match_server` 语料
- **评测指标**：完成率（人工判定）、平均步数、平均 token 成本

## 评测结果

| # | 任务 | 完成 | 说明 |
|---|---|---|---|
| 1 | 列文件 + 找心跳检测 + 读常量 | ✅ | 多步：list → read → 提取 |
| 2 | 对比两个文件的匹配逻辑 | ✅ | 读两个文件 + 对比 |
| 3 | 跨文件链路串联 | ❌ | 达到最大轮数（8 轮不够） |
| 4 | 读文件 + 写 summary.md | ✅ | 读 + 写闭环 |
| 5 | 读不存在的文件，找配置在哪 | ✅ | 失败回灌 + 推理 |
| 6 | 对比两种并发模型 | ✅ | 读两个文件 + 对比 + 推理 |
| 7 | 列文件 + 筛选 .py | ✅ | list → 筛选 → 读 |
| 8 | 五子棋胜负判断实现 | ✅ | 读 `check_win` 完整实现 |
| 9 | 对比两种并发服务器 | ✅ | 读两个文件 + 对比 |
| 10 | 通信协议分析 | ✅ | 多文件搜索 + 归纳 |

**优化前完成率：6/10 = 60%**
**优化后完成率：9/10 = 90%**

## 两轮优化

### 优化 1：Token 预算

- **问题**：任务 2、3、10 报“达到 token 预算上限”——任务需要读多个文件、多轮调用，10000 token 不够
- **改动**：`MAX_TOKENS` 从 `10000` 提到 `50000`
- **效果**：3 道超限任务全部跑完

### 优化 2：`read_file` 支持区间读取

- **问题**：`read_file` 只返回前 2000 字符，任务 4、8 拿不到文件后半部分的关键代码（`check_win`、超时逻辑）
- **改动**：
  - `read_file` 加 `start`、`limit` 参数，支持分段读取
  - 装饰器支持从类型注解推断类型（`int` → `integer`），并区分“必填/可选”（有默认值的参数不强制）
- **效果**：任务 4、8 能读到完整文件

## 失败分析

### 任务 3（唯一失败）

**问题**：“从客户端连接、到匹配成功、到进入房间，这条链路经过了哪些文件和函数？”

**原因**：**达到最大轮数（8 轮）**——任务需要跨 3+ 个文件串联，8 轮不够。

**下一步方向**：

- 提高 `max_turns`（8 → 12）
- 或优化 Agent 效率（减少无效工具调用）

## 总结

- **完成率从 60% 提升到 90%**，两轮优化贡献清晰
- **优化方向来自评测**，不是拍脑袋：token 超限 → 调预算；读不到后半段 → 加区间读取
- **追踪日志**记录了每步的 token、工具调用，为统计和归因提供数据

## 复现方式

```bash
uv sync
uv run python eval_agent.py