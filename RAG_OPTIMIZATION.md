# RAG 检索优化记录

## 背景

- **语料**：game_match_server（13 个 C++/Python 文件）
- **评测集**：29 道中文问题，由 LLM 生成，人工标注“答案所在文件”
- **指标**：HitRate@3 —— 前 3 个检索结果里，是否包含答案文件
- **基线**：按空行切分 + 纯向量检索，HitRate@3 = 70%

## 三步优化

### 优化一：切分策略对比

- **问题**：切分方式影响检索，但不知道哪种最好
- **方法**：实现三种切分（固定长度 500 字符、按空行、混合），各建索引、跑同一评测集
- **结果**：

| 策略 | HitRate@3 |
|---|---|
| 固定长度 | 77% |
| 按空行 | 70% |
| 混合 | 70% |

- **结论**：固定长度切分在本语料上最好，因为块大小一致，向量分布均匀。**后续优化基于 fixed 策略。**

### 优化二：Rerank

- **问题**：向量检索是粗筛，文本被压成 512 维向量后信息有损，top3 可能漏掉真正相关的块
- **方法**：两阶段检索 —— 向量粗筛 top 20 候选，再用 `BAAI/bge-reranker-base` 精排，取 top 3
- **结果**：

| 策略 | 无 rerank | 有 rerank |
|---|---|---|
| fixed | 77% | 87% |
| blank | 70% | 80% |
| mixed | 70% | 80% |

- **结论**：rerank 在所有切分策略上稳定提升约 10%，是通用改进。原理是“问题和文档一起过模型”，比分别编码的向量更准。

### 优化三：文件摘要

- **问题**：文件级问题（如“client.cpp 是干嘛的”）检索不到 —— 因为文件被切成多个代码片段，没有一块代表“整个文件”
- **方法**：调 LLM 给每个文件生成一段摘要，摘要作为一个额外块加入索引，前缀标 `[文件摘要]`
- **结果**：90% → **97%**
- **结论**：宏观问题需要“宏观的块”。摘要补上了“文件整体语义”这个缺口。

## 最终结果

| 阶段 | HitRate@3 |
|---|---|
| 基线 | 70% |
| + 切分优化 | 77% |
| + rerank | 87% |
| + 文件摘要 | **97%** |

从 70% 到 97%，共提升 27 个百分点。

## 失败分析

剩 1 道失败：`main.cpp 这个文件实现了什么功能？`

- **可能原因**：摘要内容偏普通（“入门程序”），语义不突出；或向量粗筛阶段未进入 top 20
- **后续方向**：混合检索（向量 + BM25 关键词）、查询改写

## 我的理解

- **切分对比是基础配置选型**，决定了后续优化在什么基础上进行；rerank 和文件摘要是其上的增量优化。
- **rerank 有效**，是因为它让问题和文档一起过模型，比“分别编码再算距离”的向量检索信息更全。
- **文件摘要**解决的不是“绑定文件”，而是补上“没有整体语义块”的缺口。

## 复现方式

```bash
uv sync
uv run python rag_load.py          # 建索引（fixed + 摘要）
uv run python eval_retrieval.py    # 跑评测